{
  "name": "ac-selection-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && node -e \"const fs=require('fs'); fs.cpSync('dist/assets', '../assets', {recursive: true}); fs.copyFileSync('dist/index.html', '../index.html');\""
  }
}
