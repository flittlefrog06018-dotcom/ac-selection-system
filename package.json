{
  "name": "ac-selection-system",
  "version": "2.5.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && node -e \"const fs=require('fs'); fs.mkdirSync('../dist', {recursive: true}); fs.cpSync('dist', '../dist', {recursive: true}); fs.cpSync('dist/assets', '../assets', {recursive: true}); fs.copyFileSync('dist/index.html', '../index.html');\""
  }
}